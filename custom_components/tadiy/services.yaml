force_refresh:
  name: Force refresh
  description: Force an immediate data refresh for TaDIY coordinator
  fields:
    entry_id:
      name: Entry ID
      description: Optional config entry ID to refresh (leave empty for all)
      required: false
      selector:
        text: